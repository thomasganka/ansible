---
# The directory into which Transformer will be downloaded for setup.
download_dir: /tmp

# The destination install directory into which Transformer and Spark
dest_dir: /opt

# SDC version and download information.
sdc_version: 3.16.2